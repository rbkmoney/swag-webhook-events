post:
  tags:
    - Event Notifications
  summary: Обработка оповещения о событии в платформе
  operationId: processWebhookEvent
  parameters:
    - $ref: '#/parameters/signature'
    - in: body
      name: event
      description: Данные произошедшего в платформе события
      required: true
      schema:
        $ref: '#/definitions/Event'
  responses:
    '200':
      description: Оповещение обработано
