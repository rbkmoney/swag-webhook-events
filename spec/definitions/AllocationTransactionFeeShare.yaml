description: Описание коммиссии в относительных величинах
type: object
required:
  - parts
properties:
  parts:
    $ref: '#/definitions/Rational'
  roundingMethod:
    description: Метод округления к целому числу
    type: string
    enum:
      - round_half_towards_zero
      - round_half_away_from_zero
